export const ROOT_NODE_ID = 'root';

export enum NodeDirection {
  top = 1,
  right = 2,
  bottom = 3,
  left = 4,
}

export enum RootVariant {
  BOTH_SIDE = 'both side',
  VERTICAL = 'vertical',
  HORIZONTAL = 'horizontal',
}
